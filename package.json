{
  "name": "silo-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "A multi-agent architecture workspace for product teams",
  "scripts": {
    "start": "node server.mjs",
    "worker": "node worker.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
